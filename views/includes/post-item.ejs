<article class="post-item">
  <h2><%= post.title %></h2>
  <p class="post-item-author">By <%= post.author_name %></p>
  <p><%= post.summary %></p>
  <div class="post-actions">
    <form action="/posts/<%= post.id %>/delete" method="POST">
      <button class="btn btn-alt">Delete Post</button>
    </form>

    <a href="/posts/<%= post.id %>/edit">Edit Post</a>
    <a class="btn" href="/posts/<%= post.id %>">View Post</a>
  </div>
</article>
<!-- the post.id above makes it so that the url on each post is dynamic and matches (literlaly is) the id of the post in the database -->
